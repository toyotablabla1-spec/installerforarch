
# for fast and deafault install:
```cd ~ && git clone https://github.com/toyotablabla1-spec/Archlinux.git && cd installerforarch/installer6/ && sh install.sh```

if you get "fatal: destination path 'installerforarch' already exists and is not an empty directory.", please dot use git clone if downloaded file
